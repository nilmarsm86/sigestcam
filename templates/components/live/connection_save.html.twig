<div class="row mb-3" {{ attributes }}>
    <div class="col-8">

    </div>
    <div class="col-4">
        <button
                type="button"
                class="btn btn-primary btn-lg {% if camera is null %}disabled{% endif %}"
                {% if camera is null %}disabled{% endif %}
        >Crear conexion
        </button>
    </div>
</div>