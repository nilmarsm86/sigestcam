{% extends 'content.html.twig' %}

{% block title %}Buscador general{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Buscador general</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Buscador general', href: ''},
            ],
            current: 'Buscador general'
        }) }}
    </div>

    {{ include('home/_search_list.html.twig') }}
{% endblock %}

