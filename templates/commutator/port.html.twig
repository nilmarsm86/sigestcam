{% extends 'content.html.twig' %}

{% block title %}Commutator{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Puertos del switch</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Switch', href: path('commutator_index')},
                {label: 'Puertos'},
            ],
            current: 'Detalle'
        }) }}
    </div>

    {{ include('commutator/_port_detail.html.twig', {'title': 'Puertos', 'showButton': true}) }}

{% endblock %}
