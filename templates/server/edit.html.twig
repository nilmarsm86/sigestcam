{% extends 'content.html.twig' %}

{% block title %}Editar Servidor{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Editar servidor</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Servidor', href: path('server_index')},
                {label: 'Editar'},
            ],
            current: 'Editar'
        }) }}
    </div>

    {{ include('server/_form.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
