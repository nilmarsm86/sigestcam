{% extends 'content.html.twig' %}

{% block title %}Server{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Datos del servidor</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Servidor', href: path('server_index')},
                {label: 'Detalle'},
            ],
            current: 'Detalle'
        }) }}
    </div>

    {{ include('camera/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
