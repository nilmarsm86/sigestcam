{% extends 'content.html.twig' %}

{% block title %}Editar Modem{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Editar modem</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Modem', href: path('modem_index')},
                {label: 'Editar'},
            ],
            current: 'Editar'
        }) }}
    </div>

    {{ include('camera/_form.html.twig', {'showButton': true, 'title': title}) }}

{#    {{ include('modem/_delete_form.html.twig') }}#}
{% endblock %}
