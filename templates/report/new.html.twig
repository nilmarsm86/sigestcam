{% extends 'content.html.twig' %}

{% block title %}New Report{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Crear reporte</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Reporte', href: path('report_index')},
                {label: 'Nuevo'},
            ],
            current: 'Nuevo'
        }) }}
    </div>
    {{ include('report/_form.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
