{% extends 'content.html.twig' %}

{% block title %}Editar Reporte{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Editar reporte</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Reportes', href: path('report_index')},
                {label: 'Editar'},
            ],
            current: 'Editar'
        }) }}
    </div>

    {{ include('report/_form.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
