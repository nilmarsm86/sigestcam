{% extends 'content.html.twig' %}

{% block title %}Listado de conexiones completas{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Listado de conexiones completas</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Conexiones', href: path('connection_index')},
                {label: 'Conexion completa', href: '#'},
                {label: 'Listado'},
            ],
            current: 'Listado'
        }) }}
    </div>

    {{ component('ConnectionList', {
        title: 'Conexiones completas',
        newHref: path('connection_full_new'),
        connection: constant('App\\Entity\\Enums\\ConnectionType::Full'),
        filter: filter
    }) }}
{% endblock %}
