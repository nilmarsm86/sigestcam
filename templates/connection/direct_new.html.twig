{% extends 'content.html.twig' %}

{% block title %}Conexiones{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Nueva conexión directa</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Conexiones', href: path('connection_index')},
                {label: 'Conexion directa', href: '#'},
                {label: 'Nueva'},
            ],
            current: 'Nueva'
        }) }}
    </div>

    {{ component('ConnectionCommutator', {
        connection: constant('App\\Entity\\Enums\\ConnectionType::Direct')
    }) }}

    {{ component('ConnectionCamera', {
        connection: constant('App\\Entity\\Enums\\ConnectionType::Direct')
    }) }}

    {{ component('ConnectionSave', {
        connection: constant('App\\Entity\\Enums\\ConnectionType::Direct')
    }) }}
{% endblock %}
