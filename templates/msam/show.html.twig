{% extends 'content.html.twig' %}

{% block title %}Msam{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-8">
            <h3 class="text-dark mb-4">Datos del msam</h3>
        </div>
        {{ component('Breadcrumb', {
            options: [
                {label: 'Inicio', href: path('app_home')},
                {label: 'Msam', href: path('msam_index')},
                {label: 'Detalle'},
            ],
            current: 'Detalle'
        }) }}
    </div>

    {{ include('msam/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
